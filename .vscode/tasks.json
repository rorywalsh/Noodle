{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "buildHTMLSlides",
            "type": "shell",
            "options": {
                "cwd": "${fileDirname}",
            },
            // "command": "python ../buildSlides.py ${file}",
            "command": "python3 ../buildSlides2.py ${file}",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}